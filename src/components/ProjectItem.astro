---
import type { CollectionEntry } from "astro:content";

type Props = {
  project: CollectionEntry<"projects">;
};
const { project }: Props = Astro.props;
---

<div class="mb-6">
  <a
    href=`/projects/${project.slug}`
    class="hover:opacity-75 transition-all duration-200 ease-out leading-relaxed"
  >
    <h1 class="font-bold text-xl md:text-xl">{project.data.title}</h1>
    <div
      class="flex items-center space-x-2 text-dark/75 dark:text-light/75 pb-1"
    >
      <!-- <p>{post.data.publishDate.toLocaleDateString()}</p> -->
      <!-- <p>•</p> -->
      <!-- <p>{post.data.readingTime} min read</p> -->
    </div>
    <p class="text-lg">{project.data.description}</p>
  </a>
</div>
